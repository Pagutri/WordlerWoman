/* eslint-disable no-undef */
// Esta clase solo es para leer la palabra del usuario. 
class GetWord {
    static listenWord() {
        const wordUser = "sales";
        return wordUser;
    }
}

module.exports = GetWord;